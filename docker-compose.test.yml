# Simple, self-contained docker compose file used for smoke testing Docker/Compose.
# It intentionally avoids project dependencies and should run quickly.
services:
  hello:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'compose-smoke-test: ok'"]
