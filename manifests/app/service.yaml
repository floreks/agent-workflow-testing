apiVersion: v1
kind: Service
metadata:
  name: agent-workflow-testing-site
  namespace: agent-workflow-testing
spec:
  selector:
    app.kubernetes.io/name: agent-workflow-testing-site
  ports:
    - name: http
      port: 80
      targetPort: 80
