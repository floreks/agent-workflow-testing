# downloadServiceManifests report: `mgmt/test-example`

This report was generated by running the Plural MCP tool `downloadServiceManifests` for:

- **Cluster:** `mgmt`
- **Service:** `test-example`

## Output location

The tool wrote rendered Kubernetes manifests to:

- `/plural/shared/manifests/mgmt-test-example`

## Files downloaded

`fileCount: 2`

- `my-web-app.yaml`
- `webapp-crd.yaml`

## High-level contents

### `my-web-app.yaml`

- Defines a custom resource **`WebApp`** (`apiVersion: example.com/v1`) named `my-web-app` in `default`.
- Spec highlights:
  - `image: nginx:1.21`
  - `replicas: 3`
  - `port: 80`
  - env vars: `ENV=production`, `LOG_LEVEL=info`
  - resource requests/limits configured.
- Includes a `status` section indicating `phase: Running` with `readyReplicas: 3`.

### `webapp-crd.yaml`

- Defines the **CustomResourceDefinition** `webapps.example.com`.
- CRD registers `WebApp` (namespaced) for group `example.com`, version `v1`.
- OpenAPI v3 schema includes `spec` fields (`image`, `replicas`, `port`, `environment`, `resources`) and `status` fields (`phase`, `message`, `readyReplicas`).

## Notes

- The downloaded artifacts appear to be **fully rendered** manifests (not Helm templates).
- The manifests were downloaded outside the git repo under `/plural/shared/manifests/` as expected by the tool.
